För att köra behöver man först köra filen make_csolve.m för att kompilera
CVXgen coden. (Man måste köra en addpath() på cvxgen mappen först).

För att köra simuleringen så måste alla parametrar defineras genom köra
MPC_setup.m

Simulink modellen finns sedan i MPC_regul.slx